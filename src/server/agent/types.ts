export interface AgentIdentity {
  id: string;
  name: string;
  title: string;
  role: string;
  goal: string;
}

export interface AgentConfig {
  name: string;
  title: string;
  role: string;
  goal: string;
}
